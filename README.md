# Maze Framework

一个现代化的迷宫框架展示网站。

## 🚀 在线预览

访问：`https://[你的GitHub用户名].github.io/[仓库名]/`

## 📦 本地开发

由于使用了 ES6 模块和动态组件加载，需要通过 HTTP 服务器运行：

```bash
# 使用 Python 3
cd maze_web
python -m http.server 8000

# 或使用 Node.js
npx http-server maze_web -p 8000
```

然后访问 `http://localhost:8000`

## 🌐 部署到 GitHub Pages

本项目已配置自动部署。当你推送到 `main` 分支时，GitHub Actions 会自动部署网站。

### 首次部署步骤：

1. 将代码推送到 GitHub
2. 进入仓库的 **Settings** > **Pages**
3. 在 **Source** 下选择 **GitHub Actions**
4. 等待自动部署完成

## 📁 项目结构

```
maze_web/
├── index.html          # 主页
├── cookbook.html       # Cookbook 页面
├── css/               # 样式文件
├── js/                # JavaScript 文件
├── components/        # HTML 组件
└── assets/           # 图片资源
```

## 🛠️ 技术栈

- 纯 HTML/CSS/JavaScript
- 无需构建工具
- 响应式设计
- 动态组件加载

## 📝 License

MIT




